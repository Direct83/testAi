# Пример для локального запуска (в GitHub Actions ключ берётся из Secrets)
# Заполняйте значения без кавычек.

# OpenAI
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-5

# GitHub
# Формат Owner/Repo, нужно только для локального запуска
GITHUB_REPOSITORY=Owner/Repo
# Базовая ветка, в которую будет открыт PR
BASE_BRANCH=main
# Токен для MCP GitHub server. Локально используйте PAT с правами repo.
# В CI пробрасывается встроенный GITHUB_TOKEN, можно не менять.
GITHUB_PERSONAL_ACCESS_TOKEN=
# Альтернативно можно задать один из этих вариантов (скрипт подхватит любой)
GITHUB_TOKEN=
GH_TOKEN=